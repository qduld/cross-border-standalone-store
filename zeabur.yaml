services:
  # Next.js Application
  - type: prebuilt
    name: craft-shop-frontend
    region: hk  # Hong Kong region, close to China
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    buildCommand: npm run build
    startCommand: npm start